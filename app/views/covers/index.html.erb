<% content_for :body_custom_style, "id='home'" %>

  <div id="myCarousel" class="carousel slide">
      <ol class="carousel-indicators">
      	<% @covers.each_with_index do |cover, index| %>
        	<li data-target="#myCarousel" data-slide-to="<%= index %>" class="<%= active_cover(index) %>"></li>        
        <% end %>

      </ol>
      <!-- Carousel items -->
      <div class="carousel-inner">
      	<% @covers.each_with_index do |cover, index | %>
        <div class="<%= active_cover(index) %> item">
          <%= image_tag cover.image.url %>
          <div class="carousel-caption pos2" style="height:30%;">
            <h1><a href="" title="View Product" style="color: rgb(249, 94, 89); cursor: pointer;">Colorized</a></h1>
          </div>
        </div>
        <% end %>        
      </div>
      <!-- Carousel nav -->
      <a class="carousel-control left" href="#myCarousel" data-slide="prev">&lsaquo;</a>
      <a class="carousel-control right" href="#myCarousel" data-slide="next">&rsaquo;</a>
   </div>    